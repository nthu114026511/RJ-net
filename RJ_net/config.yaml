# 物理與網格參數
physics:
  x_min: -5.0
  x_max: 5.0
  t_min: 0.0
  t_max: 2.0
  nx: 201  # 空間網格點數
  nt: 101  # 時間步數
  
  # Fisher-KPP 旅行波參數
  # ∂ρ/∂t = D·∂²ρ/∂x² + k⁺ρ - k⁻ρ²
  nu: 1.0          # 擴散係數 D = 1
  k_plus: 1.0      # 反應係數 k⁺ = 1
  k_minus: 1.0     # 反應係數 k⁻ = 1 (故 r(ρ) = ρ(1-ρ))
  # 波速 s = 5/√6
  # 特徵長度 L = √6
  # 精確解: ρ(x,t) = [1 + exp(-(x - s·t - x₀)/L)]⁻²

# 神經網路與訓練參數
training:
  learning_rate: 0.001
  epochs: 2000
  hidden_size: 32
  
  # 損失函數權重
  weight_pde: 1.0
  weight_bc: 0.1
  weight_ic: 1.0
  weight_reaction: 1.0

# 視覺化參數
visualization:
  animation_fps: 25

# 儲存路徑
paths:
  # 結果將儲存到本地的 results 資料夾中
  output_dir: './results'
